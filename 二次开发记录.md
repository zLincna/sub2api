# 二次开发记录

本文记录本地二次开发过程中已经完成的改动、启动方式、验证结果和后续注意事项，方便后续部署、排查和交接。

## 2026-06-12 本地启动与注册验证调整

### 一、本地项目启动情况

本地已成功跑起项目：

- 前端地址：`http://localhost:3000/`
- 后端地址：`http://127.0.0.1:8080`
- 后端健康检查：`http://127.0.0.1:8080/health`

管理员账号：

```text
admin@sub2api.local
```

管理员密码：

```text
<本地初始化时生成或自行设置的管理员密码>
```

已验证：

- `GET /health` 返回 `200`，内容为 `{"status":"ok"}`
- `GET /setup/status` 返回 `needs_setup: false`
- 管理员登录接口返回 `200`
- 前端通过 Vite 代理访问后端接口正常

### 二、本地启动过程中修复的问题

#### 1. Go 编译失败：`randomHex redeclared`

后端启动时出现编译错误：

```text
internal/service/phone_verification.go:351:6: randomHex redeclared in this block
internal/service/gemini_messages_compat_service.go:2242:6: other declaration of randomHex
```

原因：

同一个 Go package `service` 中存在两个同名函数 `randomHex`。

处理：

- 将手机验证相关文件中的 `randomHex` 改名为 `randomPhoneVerificationHex`
- 同步修改短信验证码发送中生成 `outID` 和阿里云签名随机串的调用点

相关文件：

```text
backend/internal/service/phone_verification.go
```

#### 2. Ent 生成代码与 schema 不同步

后端运行时出现 Ent runtime panic：

```text
panic: runtime error: index out of range [1] with length 1
backend/ent/runtime/runtime.go
```

原因：

`backend/ent/schema/user.go` 中已有手机号相关字段，但 `backend/ent/...` 生成代码未同步。

处理：

```bash
cd backend
go generate ./ent
```

影响文件：

```text
backend/ent/migrate/schema.go
backend/ent/mutation.go
backend/ent/runtime/runtime.go
backend/ent/user.go
backend/ent/user/user.go
backend/ent/user/where.go
backend/ent/user_create.go
backend/ent/user_update.go
```

#### 3. 本地 PostgreSQL 初始化

本机已有 PostgreSQL 和 Redis 服务，但 PostgreSQL 中不存在 `sub2api` 角色。

已创建：

```text
role: sub2api
password: sub2api
database: sub2api
```

后端首次启动时已自动执行数据库迁移，并创建管理员用户。

#### 4. pnpm 11 构建脚本审批

前端依赖安装时 pnpm 11 拦截了部分依赖的 postinstall：

```text
Ignored build scripts: esbuild@0.21.5, vue-demi@0.14.10
```

处理：

```bash
cd frontend
pnpm approve-builds --all
pnpm install
```

并新增：

```text
frontend/pnpm-workspace.yaml
```

用于允许：

```yaml
allowBuilds:
  esbuild: true
  vue-demi: true
```

同时将原先 `package.json` 中 pnpm 11 不再读取的 `overrides` 移到 workspace 配置中。

### 三、手机验证注册现状

当前代码已接入手机号注册与短信验证码验证流程。

后端支持：

- 注册请求字段：
  - `phone`
  - `phone_verify_code`
- 发送手机验证码接口：

```text
POST /api/v1/auth/send-phone-verify-code
```

- 手机号格式校验：
  - 当前按中国大陆手机号处理
  - 存储为 11 位数字
- 手机号唯一性校验
- 数据库字段：
  - `users.phone`
  - `users.phone_verified_at`
- 数据库唯一索引：
  - `users_phone_unique_active`
- 阿里云短信发送与校验

相关文件：

```text
backend/internal/service/phone_verification.go
backend/internal/service/auth_service.go
backend/internal/handler/auth_handler.go
backend/internal/server/routes/auth.go
backend/migrations/151_add_user_phone_registration.sql
frontend/src/views/auth/RegisterView.vue
frontend/src/views/auth/EmailVerifyView.vue
frontend/src/api/auth.ts
frontend/src/types/index.ts
```

### 四、手机验证码注册业务流程

#### 1. 发送验证码

前端请求：

```http
POST /api/v1/auth/send-phone-verify-code
Content-Type: application/json

{
  "phone": "13800138000"
}
```

后端执行：

1. 检查 `registration_enabled`
2. 检查 `phone_verify_enabled`
3. 校验手机号格式
4. 检查手机号是否已注册
5. 读取阿里云短信配置
6. 调用阿里云短信接口发送验证码
7. 在进程内保存验证码挑战记录：

```text
手机号 -> OutID + 过期时间
```

#### 2. 用户注册并提交验证码

注册请求示例：

```http
POST /api/v1/auth/register
Content-Type: application/json

{
  "email": "user@example.com",
  "password": "test123456",
  "phone": "13800138000",
  "phone_verify_code": "123456"
}
```

后端执行：

1. 归一化手机号
2. 检查手机验证码是否为空
3. 检查之前是否发送过验证码
4. 检查验证码挑战是否过期
5. 调用阿里云接口校验验证码是否正确
6. 校验通过后删除本地挑战记录，防止重复使用
7. 创建用户并写入：
   - `users.phone`
   - `users.phone_verified_at`

### 五、当前配置状态

本地已打开：

```json
{
  "registration_enabled": true,
  "phone_verify_enabled": true
}
```

注意：

阿里云短信参数尚未配置完整，因此调用发送手机验证码接口会返回：

```text
ALIYUN_SMS_NOT_CONFIGURED
aliyun sms service not configured
```

这表示开关已经生效，但短信服务配置未填写，不是代码流程未接入。

需要配置的阿里云短信参数：

```text
ALIYUN_ACCESS_KEY_ID
ALIYUN_ACCESS_KEY_SECRET
ALIYUN_SMS_SIGN_NAME
ALIYUN_SMS_TEMPLATE_CODE
ALIYUN_SMS_TEMPLATE_PARAM_KEY
ALIYUN_SMS_SCHEME_NAME
```

其中验证码变量名一般为：

```text
code
```

### 六、后台设置页调整

用户要求：

- 邮箱仍然作为登录账号
- 手机号作为注册资料保存
- 手机验证码作为独立可配置开关
- 开启手机验证码后，注册时必须填写手机号和短信验证码
- 手机验证开启以后，可以点击按钮配置短信参数
- 点击按钮后弹窗展示
- 弹窗里填写阿里云短信相关参数

已完成：

- `开放注册` 保持为独立开关
- `邮箱验证` 和 `手机验证` 为两个独立开关，可以按需分别开启
- 开启 `手机验证` 后显示 `配置短信` 按钮
- 点击 `配置短信` 弹出 `阿里云短信配置` 弹窗
- 弹窗中可填写：
  - `AccessKey ID`
  - `AccessKey Secret`
  - `短信签名`
  - `模板 Code`
  - `验证码变量名`
  - `SchemeName`
  - `验证码有效期（秒）`
  - `重发间隔（秒）`
  - `模板固定参数 JSON`
- 阿里云短信参数仍随原有设置保存接口保存，不新增后端接口
- 如果密钥已配置，密钥输入框显示“密钥已配置，留空以保留当前值”

相关文件：

```text
frontend/src/views/admin/SettingsView.vue
frontend/src/views/admin/__tests__/SettingsView.spec.ts
```

### 七、后台设置页测试覆盖

新增测试覆盖：

1. 邮箱验证和手机验证可以独立开启
   - 初始邮箱验证开启时，手机验证未开启
   - 开启手机验证后，保存 payload 为：

```json
{
  "email_verify_enabled": true,
  "phone_verify_enabled": true
}
```

2. 手机验证开启时可打开阿里云短信配置弹窗
   - 弹窗展示现有阿里云短信配置
   - 密钥已配置时显示保留提示
   - 修改短信参数后，保存 payload 会包含新值

### 八、验证命令与结果

已执行：

```bash
cd frontend
pnpm vitest run src/views/admin/__tests__/SettingsView.spec.ts
```

结果：

```text
20 tests passed
```

已执行：

```bash
cd frontend
pnpm typecheck
```

结果：

```text
vue-tsc --noEmit passed
```

已验证前端仍可访问：

```text
http://localhost:3000/
```

返回 `200 OK`。

### 九、后续注意事项

#### 1. 阿里云短信配置

如果要实际发送短信，必须先在后台弹窗或环境变量中配置阿里云短信参数。

配置不完整时，发送验证码会返回：

```text
ALIYUN_SMS_NOT_CONFIGURED
```

#### 2. 验证码挑战记录当前在内存中

当前手机验证码挑战记录保存在后端进程内存中：

```text
smsChallenges map
```

影响：

- 单机部署可用
- 服务重启后，已发送但未验证的验证码会失效
- 多实例部署时，如果发送验证码和注册校验落到不同实例，可能校验失败

如果后续要多副本部署，建议将验证码挑战记录迁移到 Redis。

#### 3. 保存设置的行为

阿里云短信配置弹窗只是编辑表单字段，实际提交仍使用页面底部的“保存设置”按钮。

#### 4. 工作区状态

当前工作区除本次后台设置页改动外，还包含为了本地跑起项目而产生的后端生成代码、依赖配置等变更。提交前建议按功能拆分 commit：

1. 本地启动修复和 Ent 生成代码同步
2. 后台注册验证方式 UI 调整
3. pnpm 11 workspace/build scripts 配置
